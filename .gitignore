# Secrets — never commit
.env
token.pickle

# Large intermediate caches — regenerated by the pipeline, not needed in repo
vendors_researched.json
vendors_classified.json
vendors_qa.json
vendors_insights.json

# Python
__pycache__/
*.pyc
.DS_Store
